<h1> <%= @car.brand %> </h1>
<h2> <%= @car.model %> </h2>
<p> <%= @car.fuel %>
<p> <%= @car.year %>

<%# When you select a car to add to yyour favourites, if not already added in "My Garage" an option to add it will available. If not then a statement "this car is already in your garage" will appear %>

<% if @car.favourites.empty? %>
  <%= simple_form_for [@car, @favourite] do |f| %>
    <%= f.submit 'Save in my garage' %>
  <% end %>
<% else %>
  <p>This car is already in your garage!</p>
<% end %>

<hr>
<%# Create a review form with comment and rating %>
<%= simple_form_for [@car, @review] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.submit %>
<% end %>
<%# reviews will be listed and appear on the page %>
<% @car.reviews.each do |review|  %>
  <p><%= review.comment %></>
  <p><%= review.rating %></p>
  --------------------------
<% end %>
<hr>
<%# Link to My Garage via Favourites path %>
<%= link_to "my garage", favourites_path %>
<hr>
